// @ts-check
// Centralized logging for PDF v3
// Re-exports logging functions with shorter import paths

// @ts-ignore - Module resolution issue, but file exists at runtime
export { log, logWarn, logError } from '../../../../utils/logging.js';

// Optional: Add PDF-specific logging helpers if needed
// For now, just re-export to reduce import path length







